#include <iostream>
#include <stdio.h>
#include <vector>
#include <list>
#include <limits>
#include <cmath>
#include <iomanip>
#include <string>

using namespace std;

int main(){
    string line;
    getline(cin, line);
    char s;
    scanf("%c", &s); 
    cout << setprecision(10)  << M_PI << endl;
    return 0;
}
